const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const u = await prisma.user.findMany({ select: { email: true }}); console.log('Emails de BD:', u); } main().finally(()=>prisma.$disconnect());
